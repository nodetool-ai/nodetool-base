# This file is auto-generated by nodetool.dsl.codegen.
# Please do not edit this file manually.

# Instead, edit the node class in the source module and run the following commands to regenerate the DSL:
# nodetool package scan
# nodetool codegen

from pydantic import BaseModel, Field
import typing
from typing import Any
import nodetool.metadata.types
import nodetool.metadata.types as types
from nodetool.dsl.graph import GraphNode, SingleOutputGraphNode

import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.search.google_extra
from nodetool.workflows.base_node import BaseNode

class GoogleAutocomplete(GraphNode[nodetool.nodes.search.google_extra.GoogleAutocomplete.OutputType]):
    """

        Get Google Autocomplete suggestions for a search query.
        google, autocomplete, suggestions, search, typeahead, completion
    """

    query: str | OutputHandle[str] = connect_field(default='', description='Partial search query to get completions for')
    language: str | OutputHandle[str] = connect_field(default='', description="Two-letter language code (e.g., 'en', 'de', 'fr')")
    country: str | OutputHandle[str] = connect_field(default='', description="Two-letter country code (e.g., 'us', 'de', 'fr')")

    @property
    def out(self) -> "GoogleAutocompleteOutputs":
        return GoogleAutocompleteOutputs(self)

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.search.google_extra.GoogleAutocomplete

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()

class GoogleAutocompleteOutputs(OutputsProxy):
    @property
    def results(self) -> OutputHandle[list[dict]]:
        return typing.cast(OutputHandle[list[dict]], self['results'])

    @property
    def text(self) -> OutputHandle[str]:
        return typing.cast(OutputHandle[str], self['text'])


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.search.google_extra
from nodetool.workflows.base_node import BaseNode

class GoogleEvents(GraphNode[nodetool.nodes.search.google_extra.GoogleEvents.OutputType]):
    """

        Search Google Events for local events, concerts, festivals, and activities.
        google, events, concerts, festivals, activities, local
    """

    query: str | OutputHandle[str] = connect_field(default='', description="Event search query (e.g., 'concerts in Austin')")
    location: str | OutputHandle[str] = connect_field(default='', description='Location for event search')
    num_results: int | OutputHandle[int] = connect_field(default=10, description='Maximum number of results')

    @property
    def out(self) -> "GoogleEventsOutputs":
        return GoogleEventsOutputs(self)

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.search.google_extra.GoogleEvents

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()

class GoogleEventsOutputs(OutputsProxy):
    @property
    def results(self) -> OutputHandle[list[dict]]:
        return typing.cast(OutputHandle[list[dict]], self['results'])

    @property
    def text(self) -> OutputHandle[str]:
        return typing.cast(OutputHandle[str], self['text'])


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.search.google_extra
from nodetool.workflows.base_node import BaseNode

class GoogleFlights(GraphNode[nodetool.nodes.search.google_extra.GoogleFlights.OutputType]):
    """

        Search Google Flights for airline tickets, prices, and flight options.
        google, flights, airline, tickets, travel, booking
    """

    departure_id: str | OutputHandle[str] = connect_field(default='', description="Departure airport code (e.g., 'JFK', 'LAX') or location kgmid")
    arrival_id: str | OutputHandle[str] = connect_field(default='', description="Arrival airport code (e.g., 'LHR', 'CDG') or location kgmid")
    outbound_date: str | OutputHandle[str] = connect_field(default='', description='Departure date in YYYY-MM-DD format')
    return_date: str | OutputHandle[str] = connect_field(default='', description='Return date in YYYY-MM-DD format (leave empty for one-way)')
    trip_type: int | OutputHandle[int] = connect_field(default=1, description='Trip type: 1 = Round trip, 2 = One way, 3 = Multi-city')
    adults: int | OutputHandle[int] = connect_field(default=1, description='Number of adult passengers')
    travel_class: int | OutputHandle[int] = connect_field(default=1, description='Cabin class: 1 = Economy, 2 = Premium economy, 3 = Business, 4 = First')
    currency: str | OutputHandle[str] = connect_field(default='USD', description="Currency code for prices (e.g., 'USD', 'EUR', 'GBP')")
    stops: int | OutputHandle[int] = connect_field(default=0, description='Maximum number of stops: 0 = any, 1 = non-stop, 2 = up to 1 stop, 3 = up to 2 stops')
    sort_by: int | OutputHandle[int] = connect_field(default=1, description='Sort order: 1 = Top, 2 = Price, 3 = Departure, 4 = Arrival, 5 = Duration, 6 = Emissions')

    @property
    def out(self) -> "GoogleFlightsOutputs":
        return GoogleFlightsOutputs(self)

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.search.google_extra.GoogleFlights

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()

class GoogleFlightsOutputs(OutputsProxy):
    @property
    def results(self) -> OutputHandle[list[dict]]:
        return typing.cast(OutputHandle[list[dict]], self['results'])

    @property
    def text(self) -> OutputHandle[str]:
        return typing.cast(OutputHandle[str], self['text'])


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.search.google_extra
from nodetool.workflows.base_node import BaseNode

class GoogleHotels(GraphNode[nodetool.nodes.search.google_extra.GoogleHotels.OutputType]):
    """

        Search Google Hotels for accommodations, pricing, and availability.
        google, hotels, accommodation, travel, booking, lodging
    """

    query: str | OutputHandle[str] = connect_field(default='', description="Hotel search query (e.g., 'hotels in Paris')")
    check_in_date: str | OutputHandle[str] = connect_field(default='', description='Check-in date in YYYY-MM-DD format')
    check_out_date: str | OutputHandle[str] = connect_field(default='', description='Check-out date in YYYY-MM-DD format')
    adults: int | OutputHandle[int] = connect_field(default=2, description='Number of adults')
    children: int | OutputHandle[int] = connect_field(default=0, description='Number of children')
    currency: str | OutputHandle[str] = connect_field(default='USD', description="Currency code for prices (e.g., 'USD', 'EUR', 'GBP')")
    sort_by: int | OutputHandle[int] = connect_field(default=0, description='Sort order: 0 = default, 3 = lowest price, 8 = highest rating, 13 = most reviewed')
    min_price: int | OutputHandle[int] = connect_field(default=0, description='Minimum price per night filter')
    max_price: int | OutputHandle[int] = connect_field(default=0, description='Maximum price per night filter')
    hotel_class: int | OutputHandle[int] = connect_field(default=0, description='Minimum star rating: 2-5 (0 = any)')

    @property
    def out(self) -> "GoogleHotelsOutputs":
        return GoogleHotelsOutputs(self)

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.search.google_extra.GoogleHotels

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()

class GoogleHotelsOutputs(OutputsProxy):
    @property
    def results(self) -> OutputHandle[list[dict]]:
        return typing.cast(OutputHandle[list[dict]], self['results'])

    @property
    def text(self) -> OutputHandle[str]:
        return typing.cast(OutputHandle[str], self['text'])


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.search.google_extra
from nodetool.workflows.base_node import BaseNode

class GooglePatents(GraphNode[nodetool.nodes.search.google_extra.GooglePatents.OutputType]):
    """

        Search Google Patents for patent documents, filings, and prior art.
        google, patents, intellectual property, inventions, prior art, filings
    """

    query: str | OutputHandle[str] = connect_field(default='', description='Patent search query (keywords, patent numbers, or concepts)')
    num_results: int | OutputHandle[int] = connect_field(default=10, description='Number of results per page (10-100)')
    sort: str | OutputHandle[str] = connect_field(default='', description="Sort order: 'new' for newest first, 'old' for oldest first, empty for relevance")
    inventor: str | OutputHandle[str] = connect_field(default='', description='Filter by inventor name(s), comma-separated')
    assignee: str | OutputHandle[str] = connect_field(default='', description='Filter by assignee/company name(s), comma-separated')
    country: str | OutputHandle[str] = connect_field(default='', description="Filter by country code(s), comma-separated (e.g., 'US,EP,WO')")
    status: str | OutputHandle[str] = connect_field(default='', description="Filter by status: 'GRANT' or 'APPLICATION'")
    language: str | OutputHandle[str] = connect_field(default='', description="Filter by language (e.g., 'ENGLISH', 'GERMAN', 'CHINESE')")
    before_date: str | OutputHandle[str] = connect_field(default='', description="Maximum date filter in format type:YYYYMMDD (e.g., 'publication:20230101')")
    after_date: str | OutputHandle[str] = connect_field(default='', description="Minimum date filter in format type:YYYYMMDD (e.g., 'filing:20200101')")

    @property
    def out(self) -> "GooglePatentsOutputs":
        return GooglePatentsOutputs(self)

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.search.google_extra.GooglePatents

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()

class GooglePatentsOutputs(OutputsProxy):
    @property
    def results(self) -> OutputHandle[list[dict]]:
        return typing.cast(OutputHandle[list[dict]], self['results'])

    @property
    def text(self) -> OutputHandle[str]:
        return typing.cast(OutputHandle[str], self['text'])


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.search.google_extra
from nodetool.workflows.base_node import BaseNode

class GoogleVideos(GraphNode[nodetool.nodes.search.google_extra.GoogleVideos.OutputType]):
    """

        Search Google Videos for video content across the web.
        google, videos, search, media, clips, streaming
    """

    query: str | OutputHandle[str] = connect_field(default='', description='Video search query')
    num_results: int | OutputHandle[int] = connect_field(default=10, description='Maximum number of video results (controls pagination via start offset)')
    location: str | OutputHandle[str] = connect_field(default='', description='Geographic location for search')

    @property
    def out(self) -> "GoogleVideosOutputs":
        return GoogleVideosOutputs(self)

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.search.google_extra.GoogleVideos

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()

class GoogleVideosOutputs(OutputsProxy):
    @property
    def results(self) -> OutputHandle[list[dict]]:
        return typing.cast(OutputHandle[list[dict]], self['results'])

    @property
    def text(self) -> OutputHandle[str]:
        return typing.cast(OutputHandle[str], self['text'])


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.search._base
from nodetool.workflows.base_node import BaseNode

class SerpNode(SingleOutputGraphNode[typing.Any], GraphNode[typing.Any]):
    """
    Base class for SerpAPI-powered search nodes.

        Provides common provider initialization and the search_raw helper.
        Subclasses implement process() and call self._search_raw() instead of
        repeating the 8-line provider boilerplate.
    """

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.search._base.SerpNode

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


