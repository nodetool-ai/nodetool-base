# This file is auto-generated by nodetool.dsl.codegen.
# Please do not edit this file manually.

# Instead, edit the node class in the source module and run the following commands to regenerate the DSL:
# nodetool package scan
# nodetool codegen

from pydantic import BaseModel, Field
import typing
from typing import Any
import nodetool.metadata.types
import nodetool.metadata.types as types
from nodetool.dsl.graph import GraphNode, SingleOutputGraphNode

import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.search.alternative_engines
from nodetool.workflows.base_node import BaseNode


class AmazonSearch(SingleOutputGraphNode[typing.Any], GraphNode[typing.Any]):
    """

    Search Amazon for product listings.
    amazon, ecommerce, products, search, shopping

    Use cases:
    - Find Amazon products
    - Compare product listings
    - Research product availability
    - Monitor pricing
    - Build price comparison tools
    """

    query: str | OutputHandle[str] = connect_field(
        default="", description="Product search query"
    )
    num_results: int | OutputHandle[int] = connect_field(
        default=10, description="Maximum number of product results to return"
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.search.alternative_engines.AmazonSearch

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.search.alternative_engines
from nodetool.workflows.base_node import BaseNode


class BingImages(SingleOutputGraphNode[typing.Any], GraphNode[typing.Any]):
    """

    Search Bing Images for image results.
    bing, images, search, visual, pictures

    Use cases:
    - Find images on Bing
    - Compare with Google Images
    - Access Bing's image index
    - Gather visual content
    - Build image search tools
    """

    query: str | OutputHandle[str] = connect_field(
        default="", description="Image search query"
    )
    num_results: int | OutputHandle[int] = connect_field(
        default=20, description="Maximum number of image results to return"
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.search.alternative_engines.BingImages

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.search.alternative_engines
from nodetool.workflows.base_node import BaseNode


class BingSearch(SingleOutputGraphNode[typing.Any], GraphNode[typing.Any]):
    """

    Search Bing for web results.
    bing, search, microsoft, web, query

    Use cases:
    - Get Bing search results
    - Compare with Google results
    - Access Microsoft's search index
    - Research Bing-specific rankings
    - Build multi-engine search tools
    """

    query: str | OutputHandle[str] = connect_field(
        default="", description="Search query"
    )
    num_results: int | OutputHandle[int] = connect_field(
        default=10, description="Maximum number of results to return"
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.search.alternative_engines.BingSearch

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.search.alternative_engines
from nodetool.workflows.base_node import BaseNode


class DuckDuckGoSearch(SingleOutputGraphNode[typing.Any], GraphNode[typing.Any]):
    """

    Search DuckDuckGo for privacy-focused web results.
    duckduckgo, search, privacy, web, query

    Use cases:
    - Get privacy-focused search results
    - Access DuckDuckGo's index
    - Compare with other search engines
    - Research neutral rankings
    - Build privacy-respecting tools
    """

    query: str | OutputHandle[str] = connect_field(
        default="", description="Search query"
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.search.alternative_engines.DuckDuckGoSearch

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.search.alternative_engines
from nodetool.workflows.base_node import BaseNode


class EbaySearch(SingleOutputGraphNode[typing.Any], GraphNode[typing.Any]):
    """

    Search eBay for product listings and auctions.
    ebay, ecommerce, products, auction, search

    Use cases:
    - Find eBay listings
    - Research auction prices
    - Monitor product availability
    - Compare marketplace prices
    - Track collectibles
    """

    query: str | OutputHandle[str] = connect_field(
        default="", description="Product search query"
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.search.alternative_engines.EbaySearch

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.search.alternative_engines
from nodetool.workflows.base_node import BaseNode


class WalmartSearch(SingleOutputGraphNode[typing.Any], GraphNode[typing.Any]):
    """

    Search Walmart for product listings.
    walmart, ecommerce, products, search, shopping

    Use cases:
    - Find Walmart products
    - Compare with Amazon/other retailers
    - Research product availability
    - Monitor Walmart inventory
    - Build retail comparison tools
    """

    query: str | OutputHandle[str] = connect_field(
        default="", description="Product search query"
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.search.alternative_engines.WalmartSearch

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.search.alternative_engines
from nodetool.workflows.base_node import BaseNode


class YahooSearch(SingleOutputGraphNode[typing.Any], GraphNode[typing.Any]):
    """

    Search Yahoo for web results.
    yahoo, search, web, query

    Use cases:
    - Get Yahoo search results
    - Compare with other engines
    - Access Yahoo's search index
    - Research Yahoo-specific rankings
    - Build multi-engine search
    """

    query: str | OutputHandle[str] = connect_field(
        default="", description="Search query"
    )
    num_results: int | OutputHandle[int] = connect_field(
        default=10, description="Maximum number of results to return"
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.search.alternative_engines.YahooSearch

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.search.alternative_engines
from nodetool.workflows.base_node import BaseNode


class YouTubeSearch(SingleOutputGraphNode[typing.Any], GraphNode[typing.Any]):
    """

    Search YouTube for video content.
    youtube, video, search, content, media

    Use cases:
    - Find YouTube videos
    - Discover content creators
    - Research video topics
    - Monitor video trends
    - Build video aggregators
    """

    query: str | OutputHandle[str] = connect_field(
        default="", description="Video search query"
    )
    num_results: int | OutputHandle[int] = connect_field(
        default=10, description="Maximum number of video results to return"
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.search.alternative_engines.YouTubeSearch

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()
