# This file is auto-generated by nodetool.dsl.codegen.
# Please do not edit this file manually.

# Instead, edit the node class in the source module and run the following commands to regenerate the DSL:
# nodetool package scan
# nodetool codegen

from pydantic import BaseModel, Field
import typing
from typing import Any
import nodetool.metadata.types
import nodetool.metadata.types as types
from nodetool.dsl.graph import GraphNode, SingleOutputGraphNode

import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.search.extended
from nodetool.workflows.base_node import BaseNode

class DuckDuckGoNews(GraphNode[nodetool.nodes.search.extended.DuckDuckGoNews.OutputType]):
    """

        Search DuckDuckGo News for recent news articles.
        duckduckgo, news, search, articles, current events, media
    """

    query: str | OutputHandle[str] = connect_field(default='', description='News search query')
    num_results: int | OutputHandle[int] = connect_field(default=10, description='Maximum number of results to return')

    @property
    def out(self) -> "DuckDuckGoNewsOutputs":
        return DuckDuckGoNewsOutputs(self)

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.search.extended.DuckDuckGoNews

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()

class DuckDuckGoNewsOutputs(OutputsProxy):
    @property
    def results(self) -> OutputHandle[list[dict]]:
        return typing.cast(OutputHandle[list[dict]], self['results'])

    @property
    def text(self) -> OutputHandle[str]:
        return typing.cast(OutputHandle[str], self['text'])


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.search.extended
from nodetool.workflows.base_node import BaseNode

class GoogleScholarAuthor(SingleOutputGraphNode[dict], GraphNode[dict]):
    """

        Get detailed information about a Google Scholar author by author ID, including articles and citation metrics.
        google, scholar, author, academic, researcher, citations, h-index, publications
    """

    author_id: str | OutputHandle[str] = connect_field(default='', description='Google Scholar author ID (found in the profile URL)')

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.search.extended.GoogleScholarAuthor

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.search._base
from nodetool.workflows.base_node import BaseNode

class SerpNode(SingleOutputGraphNode[typing.Any], GraphNode[typing.Any]):
    """
    Base class for SerpAPI-powered search nodes.

        Provides common provider initialization and the search_raw helper.
        Subclasses implement process() and call self._search_raw() instead of
        repeating the 8-line provider boilerplate.
    """

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.search._base.SerpNode

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.search.extended
from nodetool.workflows.base_node import BaseNode

class YelpReviews(GraphNode[nodetool.nodes.search.extended.YelpReviews.OutputType]):
    """

        Get reviews for a specific Yelp business by place ID.
        yelp, reviews, business, ratings, feedback, local
    """

    place_id: str | OutputHandle[str] = connect_field(default='', description="Yelp place ID (e.g. 'ED7A7vDdg8yLNKJTSVHHmg')")
    num_results: int | OutputHandle[int] = connect_field(default=10, description='Maximum number of reviews to return')

    @property
    def out(self) -> "YelpReviewsOutputs":
        return YelpReviewsOutputs(self)

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.search.extended.YelpReviews

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()

class YelpReviewsOutputs(OutputsProxy):
    @property
    def results(self) -> OutputHandle[list[dict]]:
        return typing.cast(OutputHandle[list[dict]], self['results'])

    @property
    def text(self) -> OutputHandle[str]:
        return typing.cast(OutputHandle[str], self['text'])


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.search.extended
from nodetool.workflows.base_node import BaseNode

class YouTubeVideo(SingleOutputGraphNode[dict], GraphNode[dict]):
    """

        Get detailed information about a specific YouTube video by video ID.
        youtube, video, details, metadata, views, likes, channel
    """

    video_id: str | OutputHandle[str] = connect_field(default='', description="YouTube video ID (the 'v' parameter from a YouTube URL, e.g. 'dQw4w9WgXcQ')")

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.search.extended.YouTubeVideo

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


