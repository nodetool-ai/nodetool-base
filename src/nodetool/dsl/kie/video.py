# This file is auto-generated by nodetool.dsl.codegen.
# Please do not edit this file manually.

# Instead, edit the node class in the source module and run the following commands to regenerate the DSL:
# nodetool package scan
# nodetool codegen

from pydantic import BaseModel, Field
import typing
from typing import Any
import nodetool.metadata.types
import nodetool.metadata.types as types
from nodetool.dsl.graph import GraphNode, SingleOutputGraphNode

import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.kie.video
from nodetool.workflows.base_node import BaseNode


class GrokImagineImageToVideo(
    SingleOutputGraphNode[types.VideoRef], GraphNode[types.VideoRef]
):
    """
    Generate videos from images using xAI's Grok Imagine model via Kie.ai.

        kie, grok, xai, video generation, ai, image-to-video, multimodal

        Grok Imagine can transform images into videos with coherent motion and
        synchronized background elements.

        Use cases:
        - Animate static images
        - Create videos from photos
        - Generate motion from images
    """

    Duration: typing.ClassVar[type] = (
        nodetool.nodes.kie.video.GrokImagineImageToVideo.Duration
    )

    image: types.ImageRef | OutputHandle[types.ImageRef] = connect_field(
        default=types.ImageRef(type="image", uri="", asset_id=None, data=None),
        description="The source image to animate.",
    )
    prompt: str | OutputHandle[str] = connect_field(
        default="", description="Optional text to guide the animation."
    )
    duration: nodetool.nodes.kie.video.GrokImagineImageToVideo.Duration = Field(
        default=nodetool.nodes.kie.video.GrokImagineImageToVideo.Duration.MEDIUM,
        description="Duration of the generated video.",
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.kie.video.GrokImagineImageToVideo

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.kie.video
from nodetool.workflows.base_node import BaseNode


class GrokImagineTextToVideo(
    SingleOutputGraphNode[types.VideoRef], GraphNode[types.VideoRef]
):
    """
    Generate videos from text using xAI's Grok Imagine model via Kie.ai.

        kie, grok, xai, video generation, ai, text-to-video, multimodal

        Grok Imagine can generate videos from text prompts with coherent motion
        and synchronized background elements.

        Use cases:
        - Generate videos from text descriptions
        - Create cinematic content from prompts
        - Text-to-video generation
    """

    Duration: typing.ClassVar[type] = (
        nodetool.nodes.kie.video.GrokImagineTextToVideo.Duration
    )
    Resolution: typing.ClassVar[type] = (
        nodetool.nodes.kie.video.GrokImagineTextToVideo.Resolution
    )

    prompt: str | OutputHandle[str] = connect_field(
        default="", description="The text prompt describing the video to generate."
    )
    duration: nodetool.nodes.kie.video.GrokImagineTextToVideo.Duration = Field(
        default=nodetool.nodes.kie.video.GrokImagineTextToVideo.Duration.MEDIUM,
        description="Duration of the generated video.",
    )
    resolution: nodetool.nodes.kie.video.GrokImagineTextToVideo.Resolution = Field(
        default=nodetool.nodes.kie.video.GrokImagineTextToVideo.Resolution.FULL_HD_1080P,
        description="Video resolution.",
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.kie.video.GrokImagineTextToVideo

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.kie.video
from nodetool.workflows.base_node import BaseNode


class HailuoImageToVideoPro(
    SingleOutputGraphNode[types.VideoRef], GraphNode[types.VideoRef]
):
    """
    Generate videos from images using MiniMax's Hailuo 2.3 Pro model via Kie.ai.

        kie, hailuo, minimax, video generation, ai, image-to-video, pro, high-quality

        Hailuo 2.3 Pro specializes in realistic character motion, expressive facial
        micro-expressions, and cinematic quality for image-to-video generation.

        Use cases:
        - Generate high-quality cinematic videos from images
        - Create content with expressive facial animations
        - Professional image-to-video production
        - Complex character movements from static images
    """

    Resolution: typing.ClassVar[type] = (
        nodetool.nodes.kie.video.HailuoImageToVideoPro.Resolution
    )

    image: types.ImageRef | OutputHandle[types.ImageRef] = connect_field(
        default=types.ImageRef(type="image", uri="", asset_id=None, data=None),
        description="The reference image to animate into a video.",
    )
    prompt: str | OutputHandle[str] = connect_field(
        default="", description="Optional text to guide the video generation."
    )
    resolution: nodetool.nodes.kie.video.HailuoImageToVideoPro.Resolution = Field(
        default=nodetool.nodes.kie.video.HailuoImageToVideoPro.Resolution.FULL_HD_1080P,
        description="Video resolution.",
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.kie.video.HailuoImageToVideoPro

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.kie.video
from nodetool.workflows.base_node import BaseNode


class HailuoImageToVideoStandard(
    SingleOutputGraphNode[types.VideoRef], GraphNode[types.VideoRef]
):
    """
    Generate videos from images using MiniMax's Hailuo 2.3 Standard model via Kie.ai.

        kie, hailuo, minimax, video generation, ai, image-to-video, standard, fast

        Hailuo 2.3 Standard offers efficient image-to-video generation with good quality
        and faster processing times for practical use cases.

        Use cases:
        - Generate quality videos from images efficiently
        - Quick image animation with realistic motion
        - Fast image-to-video prototyping
        - Practical video content creation
    """

    Resolution: typing.ClassVar[type] = (
        nodetool.nodes.kie.video.HailuoImageToVideoStandard.Resolution
    )

    image: types.ImageRef | OutputHandle[types.ImageRef] = connect_field(
        default=types.ImageRef(type="image", uri="", asset_id=None, data=None),
        description="The reference image to animate into a video.",
    )
    prompt: str | OutputHandle[str] = connect_field(
        default="", description="Optional text to guide the video generation."
    )
    resolution: nodetool.nodes.kie.video.HailuoImageToVideoStandard.Resolution = Field(
        default=nodetool.nodes.kie.video.HailuoImageToVideoStandard.Resolution.HD_720P,
        description="Video resolution.",
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.kie.video.HailuoImageToVideoStandard

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.kie.video
from nodetool.workflows.base_node import BaseNode


class KlingAIAvatarPro(
    SingleOutputGraphNode[types.VideoRef], GraphNode[types.VideoRef]
):
    """
    Generate talking avatar videos using Kuaishou's Kling AI via Kie.ai.

        kie, kling, kuaishou, avatar, video generation, ai, talking-head, lip-sync

        Transforms a photo plus audio track into a lip-synced talking avatar video
        with natural-looking speech animation and consistent identity.

        Use cases:
        - Create virtual influencer content
        - Generate educational presenters
        - Lip-synced avatar videos
        - Virtual spokesperson creation
    """

    Mode: typing.ClassVar[type] = nodetool.nodes.kie.video.KlingAIAvatarPro.Mode

    image: types.ImageRef | OutputHandle[types.ImageRef] = connect_field(
        default=types.ImageRef(type="image", uri="", asset_id=None, data=None),
        description="The face/character image to animate.",
    )
    audio: types.AudioRef | OutputHandle[types.AudioRef] = connect_field(
        default=types.AudioRef(type="audio", uri="", asset_id=None, data=None),
        description="The audio track for lip-syncing.",
    )
    prompt: str | OutputHandle[str] = connect_field(
        default="", description="Optional text to guide emotions and expressions."
    )
    mode: nodetool.nodes.kie.video.KlingAIAvatarPro.Mode = Field(
        default=nodetool.nodes.kie.video.KlingAIAvatarPro.Mode.STANDARD,
        description="Generation mode: 'standard' or 'pro' for higher quality.",
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.kie.video.KlingAIAvatarPro

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.kie.video
from nodetool.workflows.base_node import BaseNode


class KlingAIAvatarStandard(
    SingleOutputGraphNode[types.VideoRef], GraphNode[types.VideoRef]
):
    """
    Generate talking avatar videos using Kuaishou's Kling AI via Kie.ai.

        kie, kling, kuaishou, avatar, video generation, ai, talking-head, lip-sync

        Transforms a photo plus audio track into a lip-synced talking avatar video
        with natural-looking speech animation and consistent identity.

        Use cases:
        - Create virtual influencer content
        - Generate educational presenters
        - Lip-synced avatar videos
        - Virtual spokesperson creation
    """

    Mode: typing.ClassVar[type] = nodetool.nodes.kie.video.KlingAIAvatarStandard.Mode

    image: types.ImageRef | OutputHandle[types.ImageRef] = connect_field(
        default=types.ImageRef(type="image", uri="", asset_id=None, data=None),
        description="The face/character image to animate.",
    )
    audio: types.AudioRef | OutputHandle[types.AudioRef] = connect_field(
        default=types.AudioRef(type="audio", uri="", asset_id=None, data=None),
        description="The audio track for lip-syncing.",
    )
    prompt: str | OutputHandle[str] = connect_field(
        default="", description="Optional text to guide emotions and expressions."
    )
    mode: nodetool.nodes.kie.video.KlingAIAvatarStandard.Mode = Field(
        default=nodetool.nodes.kie.video.KlingAIAvatarStandard.Mode.STANDARD,
        description="Generation mode: 'standard' or 'pro' for higher quality.",
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.kie.video.KlingAIAvatarStandard

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.kie.video
from nodetool.workflows.base_node import BaseNode


class KlingImageToVideo(
    SingleOutputGraphNode[types.VideoRef], GraphNode[types.VideoRef]
):
    """
    Generate videos from images using Kuaishou's Kling 2.6 model via Kie.ai.

        kie, kling, kuaishou, video generation, ai, image-to-video, realistic

        Kling 2.6 Image-to-Video transforms reference images into high-quality videos
        with realistic motion, physics consistency, and cinematic quality output.

        Use cases:
        - Generate high-quality videos from images
        - Create realistic motion and physics from images
        - Animate static images with cinematic quality
        - Professional image-to-video production
    """

    AspectRatio: typing.ClassVar[type] = (
        nodetool.nodes.kie.video.KlingImageToVideo.AspectRatio
    )
    Resolution: typing.ClassVar[type] = (
        nodetool.nodes.kie.video.KlingImageToVideo.Resolution
    )

    image: types.ImageRef | OutputHandle[types.ImageRef] = connect_field(
        default=types.ImageRef(type="image", uri="", asset_id=None, data=None),
        description="The reference image to animate into a video.",
    )
    prompt: str | OutputHandle[str] = connect_field(
        default="", description="Optional text to guide the video generation."
    )
    aspect_ratio: nodetool.nodes.kie.video.KlingImageToVideo.AspectRatio = Field(
        default=nodetool.nodes.kie.video.KlingImageToVideo.AspectRatio.LANDSCAPE,
        description="The aspect ratio of the generated video.",
    )
    resolution: nodetool.nodes.kie.video.KlingImageToVideo.Resolution = Field(
        default=nodetool.nodes.kie.video.KlingImageToVideo.Resolution.HD_720P,
        description="Video resolution.",
    )
    duration: int | OutputHandle[int] = connect_field(
        default=5, description="Video duration in seconds."
    )
    seed: int | OutputHandle[int] = connect_field(
        default=-1,
        description="Random seed for reproducible results. Use -1 for random seed.",
    )
    sound: bool | OutputHandle[bool] = connect_field(
        default=False, description="Whether the generated video includes sound."
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.kie.video.KlingImageToVideo

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.kie.video
from nodetool.workflows.base_node import BaseNode


class KlingTextToVideo(
    SingleOutputGraphNode[types.VideoRef], GraphNode[types.VideoRef]
):
    """
    Generate videos from text using Kuaishou's Kling 2.6 model via Kie.ai.

        kie, kling, kuaishou, video generation, ai, text-to-video, realistic

        Kling 2.6 offers high-quality text-to-video generation with realistic motion,
        physics consistency, and cinematic quality output.

        Use cases:
        - Generate high-quality videos from text
        - Create realistic motion and physics
        - Cinematic content creation
        - Professional video production
    """

    AspectRatio: typing.ClassVar[type] = (
        nodetool.nodes.kie.video.KlingTextToVideo.AspectRatio
    )
    Resolution: typing.ClassVar[type] = (
        nodetool.nodes.kie.video.KlingTextToVideo.Resolution
    )

    prompt: str | OutputHandle[str] = connect_field(
        default="", description="The text prompt describing the video to generate."
    )
    aspect_ratio: nodetool.nodes.kie.video.KlingTextToVideo.AspectRatio = Field(
        default=nodetool.nodes.kie.video.KlingTextToVideo.AspectRatio.LANDSCAPE,
        description="The aspect ratio of the generated video.",
    )
    resolution: nodetool.nodes.kie.video.KlingTextToVideo.Resolution = Field(
        default=nodetool.nodes.kie.video.KlingTextToVideo.Resolution.HD_720P,
        description="Video resolution.",
    )
    duration: int | OutputHandle[int] = connect_field(
        default=5, description="Video duration in seconds."
    )
    seed: int | OutputHandle[int] = connect_field(
        default=-1,
        description="Random seed for reproducible results. Use -1 for random seed.",
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.kie.video.KlingTextToVideo

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.kie.video
from nodetool.workflows.base_node import BaseNode


class SeedanceV1LiteImageToVideo(
    SingleOutputGraphNode[types.VideoRef], GraphNode[types.VideoRef]
):
    """
    Generate videos from images using ByteDance's Seedance V1 Lite model via Kie.ai.

        kie, seedance, bytedance, video generation, ai, image-to-video, lite, fast

        Seedance V1 Lite offers fast image-to-video generation with efficient processing,
        transforming reference images into dynamic video sequences.

        Use cases:
        - Fast image animation from reference images
        - Quick video prototyping
        - Animate static images efficiently
        - Rapid iteration on image-to-video concepts
    """

    Resolution: typing.ClassVar[type] = (
        nodetool.nodes.kie.video.SeedanceV1LiteImageToVideo.Resolution
    )

    image: types.ImageRef | OutputHandle[types.ImageRef] = connect_field(
        default=types.ImageRef(type="image", uri="", asset_id=None, data=None),
        description="The reference image to animate into a video.",
    )
    prompt: str | OutputHandle[str] = connect_field(
        default="", description="Optional text to guide the video generation."
    )
    resolution: nodetool.nodes.kie.video.SeedanceV1LiteImageToVideo.Resolution = Field(
        default=nodetool.nodes.kie.video.SeedanceV1LiteImageToVideo.Resolution.HD_720P,
        description="Video resolution.",
    )
    duration: int | OutputHandle[int] = connect_field(
        default=5, description="Video duration in seconds."
    )
    camera_fixed: bool | OutputHandle[bool] = connect_field(
        default=False,
        description="Whether to keep the camera fixed or allow camera movement.",
    )
    seed: int | OutputHandle[int] = connect_field(
        default=-1,
        description="Random seed for reproducible results. Use -1 for random seed.",
    )
    enable_safety_checker: bool | OutputHandle[bool] = connect_field(
        default=True,
        description="Enable safety checker to filter inappropriate content.",
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.kie.video.SeedanceV1LiteImageToVideo

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.kie.video
from nodetool.workflows.base_node import BaseNode


class SeedanceV1LiteTextToVideo(
    SingleOutputGraphNode[types.VideoRef], GraphNode[types.VideoRef]
):
    """
    Generate videos from text using ByteDance's Seedance V1 Lite model via Kie.ai.

        kie, seedance, bytedance, video generation, ai, text-to-video, lite, fast

        Seedance V1 Lite offers fast text-to-video generation with efficient processing,
        supporting multi-shot videos and cinematic scene transitions.

        Use cases:
        - Fast video prototyping from text
        - Create multi-shot videos with scene transitions
        - Quick concept visualization
        - Rapid iteration on video ideas
    """

    AspectRatio: typing.ClassVar[type] = (
        nodetool.nodes.kie.video.SeedanceV1LiteTextToVideo.AspectRatio
    )
    Resolution: typing.ClassVar[type] = (
        nodetool.nodes.kie.video.SeedanceV1LiteTextToVideo.Resolution
    )

    prompt: str | OutputHandle[str] = connect_field(
        default="",
        description="The text prompt describing the video to generate. Supports shot descriptions with [Cut to] notation.",
    )
    aspect_ratio: nodetool.nodes.kie.video.SeedanceV1LiteTextToVideo.AspectRatio = (
        Field(
            default=nodetool.nodes.kie.video.SeedanceV1LiteTextToVideo.AspectRatio.LANDSCAPE,
            description="The aspect ratio of the generated video.",
        )
    )
    resolution: nodetool.nodes.kie.video.SeedanceV1LiteTextToVideo.Resolution = Field(
        default=nodetool.nodes.kie.video.SeedanceV1LiteTextToVideo.Resolution.HD_720P,
        description="Video resolution.",
    )
    duration: int | OutputHandle[int] = connect_field(
        default=5, description="Video duration in seconds."
    )
    camera_fixed: bool | OutputHandle[bool] = connect_field(
        default=False,
        description="Whether to keep the camera fixed or allow camera movement.",
    )
    seed: int | OutputHandle[int] = connect_field(
        default=-1,
        description="Random seed for reproducible results. Use -1 for random seed.",
    )
    enable_safety_checker: bool | OutputHandle[bool] = connect_field(
        default=True,
        description="Enable safety checker to filter inappropriate content.",
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.kie.video.SeedanceV1LiteTextToVideo

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.kie.video
from nodetool.workflows.base_node import BaseNode


class SeedanceV1ProFastImageToVideo(
    SingleOutputGraphNode[types.VideoRef], GraphNode[types.VideoRef]
):
    """
    Generate videos from images using ByteDance's Seedance V1 Pro Fast model via Kie.ai.

        kie, seedance, bytedance, video generation, ai, image-to-video, pro, fast

        Seedance V1 Pro Fast offers balanced image-to-video generation with both
        quality and speed, transforming reference images into high-quality video sequences
        with optimized processing time.

        Use cases:
        - Generate high-quality videos from images efficiently
        - Fast professional image animations
        - Balanced quality and speed image-to-video conversion
        - Production-ready image animation
    """

    Resolution: typing.ClassVar[type] = (
        nodetool.nodes.kie.video.SeedanceV1ProFastImageToVideo.Resolution
    )

    image: types.ImageRef | OutputHandle[types.ImageRef] = connect_field(
        default=types.ImageRef(type="image", uri="", asset_id=None, data=None),
        description="The reference image to animate into a video.",
    )
    prompt: str | OutputHandle[str] = connect_field(
        default="", description="Optional text to guide the video generation."
    )
    resolution: nodetool.nodes.kie.video.SeedanceV1ProFastImageToVideo.Resolution = (
        Field(
            default=nodetool.nodes.kie.video.SeedanceV1ProFastImageToVideo.Resolution.HD_720P,
            description="Video resolution.",
        )
    )
    duration: int | OutputHandle[int] = connect_field(
        default=5, description="Video duration in seconds."
    )
    camera_fixed: bool | OutputHandle[bool] = connect_field(
        default=False,
        description="Whether to keep the camera fixed or allow camera movement.",
    )
    seed: int | OutputHandle[int] = connect_field(
        default=-1,
        description="Random seed for reproducible results. Use -1 for random seed.",
    )
    enable_safety_checker: bool | OutputHandle[bool] = connect_field(
        default=True,
        description="Enable safety checker to filter inappropriate content.",
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.kie.video.SeedanceV1ProFastImageToVideo

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.kie.video
from nodetool.workflows.base_node import BaseNode


class SeedanceV1ProImageToVideo(
    SingleOutputGraphNode[types.VideoRef], GraphNode[types.VideoRef]
):
    """
    Generate videos from images using ByteDance's Seedance V1 Pro model via Kie.ai.

        kie, seedance, bytedance, video generation, ai, image-to-video, pro, high-quality

        Seedance V1 Pro offers high-quality image-to-video generation with improved
        fidelity, transforming reference images into professional video sequences.

        Use cases:
        - Generate high-quality videos from images
        - Create professional image animations
        - Cinematic image-to-video conversion
        - High-fidelity video production from images
    """

    Resolution: typing.ClassVar[type] = (
        nodetool.nodes.kie.video.SeedanceV1ProImageToVideo.Resolution
    )

    image: types.ImageRef | OutputHandle[types.ImageRef] = connect_field(
        default=types.ImageRef(type="image", uri="", asset_id=None, data=None),
        description="The reference image to animate into a video.",
    )
    prompt: str | OutputHandle[str] = connect_field(
        default="", description="Optional text to guide the video generation."
    )
    resolution: nodetool.nodes.kie.video.SeedanceV1ProImageToVideo.Resolution = Field(
        default=nodetool.nodes.kie.video.SeedanceV1ProImageToVideo.Resolution.HD_720P,
        description="Video resolution.",
    )
    duration: int | OutputHandle[int] = connect_field(
        default=5, description="Video duration in seconds."
    )
    camera_fixed: bool | OutputHandle[bool] = connect_field(
        default=False,
        description="Whether to keep the camera fixed or allow camera movement.",
    )
    seed: int | OutputHandle[int] = connect_field(
        default=-1,
        description="Random seed for reproducible results. Use -1 for random seed.",
    )
    enable_safety_checker: bool | OutputHandle[bool] = connect_field(
        default=True,
        description="Enable safety checker to filter inappropriate content.",
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.kie.video.SeedanceV1ProImageToVideo

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.kie.video
from nodetool.workflows.base_node import BaseNode


class SeedanceV1ProTextToVideo(
    SingleOutputGraphNode[types.VideoRef], GraphNode[types.VideoRef]
):
    """
    Generate videos from text using ByteDance's Seedance V1 Pro model via Kie.ai.

        kie, seedance, bytedance, video generation, ai, text-to-video, pro, high-quality

        Seedance V1 Pro offers high-quality text-to-video generation with improved
        fidelity, supporting multi-shot videos and cinematic scene transitions.

        Use cases:
        - Generate high-quality videos from text
        - Create professional multi-shot videos
        - Cinematic content creation
        - High-fidelity video production
    """

    AspectRatio: typing.ClassVar[type] = (
        nodetool.nodes.kie.video.SeedanceV1ProTextToVideo.AspectRatio
    )
    Resolution: typing.ClassVar[type] = (
        nodetool.nodes.kie.video.SeedanceV1ProTextToVideo.Resolution
    )

    prompt: str | OutputHandle[str] = connect_field(
        default="",
        description="The text prompt describing the video to generate. Supports shot descriptions with [Cut to] notation.",
    )
    aspect_ratio: nodetool.nodes.kie.video.SeedanceV1ProTextToVideo.AspectRatio = Field(
        default=nodetool.nodes.kie.video.SeedanceV1ProTextToVideo.AspectRatio.LANDSCAPE,
        description="The aspect ratio of the generated video.",
    )
    resolution: nodetool.nodes.kie.video.SeedanceV1ProTextToVideo.Resolution = Field(
        default=nodetool.nodes.kie.video.SeedanceV1ProTextToVideo.Resolution.HD_720P,
        description="Video resolution.",
    )
    duration: int | OutputHandle[int] = connect_field(
        default=5, description="Video duration in seconds."
    )
    camera_fixed: bool | OutputHandle[bool] = connect_field(
        default=False,
        description="Whether to keep the camera fixed or allow camera movement.",
    )
    seed: int | OutputHandle[int] = connect_field(
        default=-1,
        description="Random seed for reproducible results. Use -1 for random seed.",
    )
    enable_safety_checker: bool | OutputHandle[bool] = connect_field(
        default=True,
        description="Enable safety checker to filter inappropriate content.",
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.kie.video.SeedanceV1ProTextToVideo

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.kie.video
from nodetool.workflows.base_node import BaseNode


class Sora2ProImageToVideo(
    SingleOutputGraphNode[types.VideoRef], GraphNode[types.VideoRef]
):
    """
    Generate videos from images using OpenAI's Sora 2 Pro model via Kie.ai.

        kie, sora, openai, video generation, ai, image-to-video, pro, 1080p

        Sora 2 Pro Image-to-Video transforms reference images into professional-grade
        HD videos with improved realism in motion and physics. Supports higher resolution
        (HD) and longer clips (15s).

        Use cases:
        - Generate professional-grade HD videos from images
        - Create longer 15-second video clips
        - High-fidelity motion and physics from images
        - Premium image-to-video content creation
    """

    AspectRatio: typing.ClassVar[type] = (
        nodetool.nodes.kie.video.Sora2ProImageToVideo.AspectRatio
    )

    image: types.ImageRef | OutputHandle[types.ImageRef] = connect_field(
        default=types.ImageRef(type="image", uri="", asset_id=None, data=None),
        description="The reference image to animate into a video.",
    )
    prompt: str | OutputHandle[str] = connect_field(
        default="", description="Optional text to guide the video generation."
    )
    aspect_ratio: nodetool.nodes.kie.video.Sora2ProImageToVideo.AspectRatio = Field(
        default=nodetool.nodes.kie.video.Sora2ProImageToVideo.AspectRatio.LANDSCAPE,
        description="The aspect ratio of the generated video.",
    )
    n_frames: int | OutputHandle[int] = connect_field(
        default=10, description="Number of frames for the video."
    )
    remove_watermark: bool | OutputHandle[bool] = connect_field(
        default=True,
        description="Whether to remove the watermark from the generated video.",
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.kie.video.Sora2ProImageToVideo

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.kie.video
from nodetool.workflows.base_node import BaseNode


class Sora2ProStoryboard(
    SingleOutputGraphNode[types.VideoRef], GraphNode[types.VideoRef]
):
    """
    Generate storyboard videos using OpenAI's Sora 2 Pro model via Kie.ai.

        kie, sora, openai, video generation, ai, storyboard, pro, 1080p

        Sora 2 Pro Storyboard creates professional-grade storyboard videos with
        scene transitions, cinematic framing, and professional editing techniques.

        Use cases:
        - Generate cinematic storyboards from text
        - Create professional video previews
        - Plan video shoots with visual storyboards
        - Develop video concepts with scene sequences
    """

    AspectRatio: typing.ClassVar[type] = (
        nodetool.nodes.kie.video.Sora2ProStoryboard.AspectRatio
    )

    prompt: str | OutputHandle[str] = connect_field(
        default="", description="The text prompt describing the storyboard sequence."
    )
    aspect_ratio: nodetool.nodes.kie.video.Sora2ProStoryboard.AspectRatio = Field(
        default=nodetool.nodes.kie.video.Sora2ProStoryboard.AspectRatio.LANDSCAPE,
        description="The aspect ratio of the generated video.",
    )
    n_frames: int | OutputHandle[int] = connect_field(
        default=10, description="Number of frames for the video."
    )
    remove_watermark: bool | OutputHandle[bool] = connect_field(
        default=True,
        description="Whether to remove the watermark from the generated video.",
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.kie.video.Sora2ProStoryboard

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.kie.video
from nodetool.workflows.base_node import BaseNode


class Sora2ProTextToVideo(
    SingleOutputGraphNode[types.VideoRef], GraphNode[types.VideoRef]
):
    """
    Generate videos from text using OpenAI's Sora 2 Pro model via Kie.ai.

        kie, sora, openai, video generation, ai, text-to-video, pro, 1080p

        Sora 2 Pro Text-to-Video generates professional-grade HD videos from text prompts
        with improved realism in motion and physics. Supports higher resolution (HD) and
        longer clips (15s).

        Use cases:
        - Generate professional-grade HD videos from text
        - Create longer 15-second clips
        - High-fidelity motion and physics
        - Premium text-to-video content creation
    """

    AspectRatio: typing.ClassVar[type] = (
        nodetool.nodes.kie.video.Sora2ProTextToVideo.AspectRatio
    )

    prompt: str | OutputHandle[str] = connect_field(
        default="", description="The text prompt describing the video to generate."
    )
    aspect_ratio: nodetool.nodes.kie.video.Sora2ProTextToVideo.AspectRatio = Field(
        default=nodetool.nodes.kie.video.Sora2ProTextToVideo.AspectRatio.LANDSCAPE,
        description="The aspect ratio of the generated video.",
    )
    n_frames: int | OutputHandle[int] = connect_field(
        default=10, description="Number of frames for the video."
    )
    remove_watermark: bool | OutputHandle[bool] = connect_field(
        default=True,
        description="Whether to remove the watermark from the generated video.",
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.kie.video.Sora2ProTextToVideo

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.kie.video
from nodetool.workflows.base_node import BaseNode


class Sora2TextToVideo(
    SingleOutputGraphNode[types.VideoRef], GraphNode[types.VideoRef]
):
    """
    Generate videos from text using OpenAI's Sora 2 model via Kie.ai.

        kie, sora, openai, video generation, ai, text-to-video, realistic

        Sora 2 Text-to-Video generates short videos (up to 10 seconds) from text prompts,
        emphasizing realistic motion, physics consistency, and native audio. Supports
        standard and pro modes for different quality/speed tradeoffs.

        Use cases:
        - Generate realistic videos from text
        - Create videos with native audio (dialogue/ambient sound)
        - Quick text-to-video prototyping
        - Cinematic content creation
    """

    AspectRatio: typing.ClassVar[type] = (
        nodetool.nodes.kie.video.Sora2TextToVideo.AspectRatio
    )
    Mode: typing.ClassVar[type] = nodetool.nodes.kie.video.Sora2TextToVideo.Mode

    prompt: str | OutputHandle[str] = connect_field(
        default="", description="The text prompt describing the video to generate."
    )
    aspect_ratio: nodetool.nodes.kie.video.Sora2TextToVideo.AspectRatio = Field(
        default=nodetool.nodes.kie.video.Sora2TextToVideo.AspectRatio.LANDSCAPE,
        description="The aspect ratio of the generated video.",
    )
    n_frames: int | OutputHandle[int] = connect_field(
        default=10, description="Number of frames for the video."
    )
    remove_watermark: bool | OutputHandle[bool] = connect_field(
        default=True,
        description="Whether to remove the watermark from the generated video.",
    )
    mode: nodetool.nodes.kie.video.Sora2TextToVideo.Mode = Field(
        default=nodetool.nodes.kie.video.Sora2TextToVideo.Mode.STANDARD,
        description="Generation mode: 'standard' or 'pro' for higher quality.",
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.kie.video.Sora2TextToVideo

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()


import typing
from pydantic import Field
from nodetool.dsl.handles import OutputHandle, OutputsProxy, connect_field
import nodetool.nodes.kie.video
from nodetool.workflows.base_node import BaseNode


class TopazVideoUpscale(
    SingleOutputGraphNode[types.VideoRef], GraphNode[types.VideoRef]
):
    """
    Upscale and enhance videos using Topaz Labs AI via Kie.ai.

        kie, topaz, upscale, enhance, video, ai, 4k

        Uses Topaz Labs' video enhancement AI to upscale videos to 1080p or 4K,
        sharpen frames, reduce noise, and interpolate frames for smoother motion.

        Use cases:
        - Upscale old or low-quality footage to HD/4K
        - Enhance YouTube content or game recordings
        - Restore and improve home videos
        - Prepare videos for high-resolution displays
    """

    Resolution: typing.ClassVar[type] = (
        nodetool.nodes.kie.video.TopazVideoUpscale.Resolution
    )

    video: types.VideoRef | OutputHandle[types.VideoRef] = connect_field(
        default=types.VideoRef(
            type="video", uri="", asset_id=None, data=None, duration=None, format=None
        ),
        description="The video to upscale.",
    )
    resolution: nodetool.nodes.kie.video.TopazVideoUpscale.Resolution = Field(
        default=nodetool.nodes.kie.video.TopazVideoUpscale.Resolution.HD_1080P,
        description="Target resolution for upscaling.",
    )
    denoise: bool | OutputHandle[bool] = connect_field(
        default=True, description="Apply denoising to reduce artifacts."
    )

    @classmethod
    def get_node_class(cls) -> type[BaseNode]:
        return nodetool.nodes.kie.video.TopazVideoUpscale

    @classmethod
    def get_node_type(cls):
        return cls.get_node_class().get_node_type()
